* compaction
* parallel reads
* round-robin writing
* LastModified instead of Version
* DELETE support
* efficient range requests

* allow client-supplied keys and allow them to be strings.
** this means headers are variable length. :(
